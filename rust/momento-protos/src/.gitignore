# We generate source into this directory from proto-generator
*.rs

# Need this file to list the proto module imports
!lib.rs
!protosocket/mod.rs

# TEMP: and all code files for a git dependency right now without releasing
!*.rs
