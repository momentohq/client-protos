[package]
name = "momento-protos"
version = "0.1.0"
authors = ["Matt Straathof <eng@momentohq.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
protobuf = { version = "2", features = ["with-bytes"] }
